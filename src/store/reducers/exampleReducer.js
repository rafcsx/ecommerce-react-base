// src/store/reducers/exampleReducer.js
const initialState = {
    // Defina o estado inicial aqui
};

const exampleReducer = (state = initialState, action) => {
    switch (action.type) {
        // Adicione seus casos de ação aqui
        default:
            return state;
    }
};

export default exampleReducer;
